FROM node:20-alpine

WORKDIR /app

# Install deps first (better caching)
COPY package*.json ./
RUN npm ci || npm install

# Copy the rest
COPY . .

# Railway provides PORT
ENV NODE_ENV=production
EXPOSE 3001

CMD ["node", "server.js"]